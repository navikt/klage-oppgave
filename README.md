# NAV Oppgave

Push til main ruller ut ny versjon på

https://klage-oppgave.dev.nav.no/

Kjør opp i dev slik:

### Frontend-DEV

`cd frontend`

`npm install`

`NODE_ENV=development npm start`

### API-MOCK-DEV

`cd api-mock-server`

`npm install`

`NODE_ENV=development npm start`
